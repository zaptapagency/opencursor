// A small module for trying opencursor's inline edit and agent features.

export function add(a, b) {
  return a + b;
}

export function subtract(a, b) {
  return a - b;
}

// Try: select this function and press Cmd/Ctrl+K → "make it throw on divide by zero".
export function divide(a, b) {
  return a / b;
}
