# Normalize line endings to LF in the repo so CI on Linux (GitHub Actions,
# Railway/Nixpacks) and local Windows checkouts stay byte-consistent.
* text=auto eol=lf

# Binary assets must never be normalized.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.vsix binary
